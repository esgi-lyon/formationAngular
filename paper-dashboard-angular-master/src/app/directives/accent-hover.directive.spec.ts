import { AccentHoverDirective } from './accent-hover.directive';

describe('AccentHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AccentHoverDirective();
    expect(directive).toBeTruthy();
  });
});
