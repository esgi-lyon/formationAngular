import { AppreciationToColorPipe } from './appreciation-to-color.pipe';

describe('AppreciationToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AppreciationToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
